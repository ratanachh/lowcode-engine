# Deprecated: Alibaba CDN / npm docs publish.
# Docs now deploy to GitHub Pages via deploy-docs.yml.
#
# Keeping this file disabled so old triggers do not run.
name: Update and Publish Docs (deprecated)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type publish to run the legacy npm docs flow'
        required: true

jobs:
  skip:
    if: github.event.inputs.confirm == 'publish'
    runs-on: ubuntu-latest
    steps:
      - name: Legacy flow notice
        run: |
          echo "Legacy docs npm publish is deprecated."
          echo "Use the Deploy Docs to GitHub Pages workflow instead."
          exit 1
